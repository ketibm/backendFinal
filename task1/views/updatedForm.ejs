<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update Faculty</title>
  </head>
  <body>
    <h1>Добредојдовте на Нашиот Универзитет</h1>
    <h2>Факултети</h2>

    <form action="/update/<%= faculty._id %>" method="post">
      <input
        type="text"
        name="name"
        placeholder="Name"
        value="<%= faculty.name %>"
        required
      />
      <input
        type="text"
        name="address"
        placeholder="Address"
        value="<%= faculty.address %>"
        required
      />
      <select name="university" required>
        <% universities.forEach(university => { %>
        <option value="<%= university._id %>"
          <% if (university._id.toString() === faculty.university.toString()) {
          %> selected <% } %>><%= university.name %>
        </option>
        <% }); %>
      </select>
      <button type="submit">Update</button>
    </form>
  </body>
</html>
